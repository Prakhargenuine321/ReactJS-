//same thing we did it in ".jsx"(vite) but we need to also do here 
//the difference is often that here we can use ".js" file also 
//as we are not able to do it in "vite" app.


//function name should be in Uppercase
//best practice is to file-name should also be in Uppercase.

function Chai(){
    return(
        <h2>Chai in react</h2>
    );
}

export default Chai