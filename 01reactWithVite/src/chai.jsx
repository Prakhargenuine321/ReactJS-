//you need to always maintain the file name with ".jsx" extension,
//as we are working inside "framework" so here is always milaitry type rules.
//and always start function with Uppercase letter.

function Chai(){
    return(
        <h2>Chai is ready bro with React</h2>
    );
}

export default Chai